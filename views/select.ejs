<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h1>水族館</h1>
  <h3>id 1 2 3 4 count</h3>
  <table>
      <% for( let row of data ) { %>
        <tr>
            <td><%= row.id %></td>
            <td><%= row. Se_id %></td>
            <td><%= row. Re_id %></td>
            <td><%= row. Ma_id %></td>
            <td><%= row. Sp_id %></td>
            <td><%= row. count %></td>
        </tr>
      <% } %>
  </table>
  <a href="/">topページへ</a>
</body>
</html>